var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
_parcelHelpers.defineInteropFlag(exports);
_parcelHelpers.export(exports, "templates", function () {
  return templates;
});
var _utils = require('./utils');
function title(block) {
  return _utils.row(_utils.col(`<h1>${block.value}</h1>`));
}
function text(block) {
  return _utils.row(_utils.col(`<p>${block.value}</p>`));
}
function colums(block) {
  const html = block.value.map(item => `<div class="col-sm">${item}</div>`);
  return _utils.row(`${html.join('')}`);
}
function image(block) {
  return _utils.row(`<img src=${block.value} />`);
}
const templates = {
  title,
  text,
  colums,
  image
};
